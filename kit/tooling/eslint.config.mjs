// Point de départ — la couche "zéro token".
// À étoffer : encoder ici un maximum de règles `Vérifié par: eslint`
// (longueur de fonctions, complexité, no-explicit-any, import boundaries Nest, hooks rules React...).
export default [];
